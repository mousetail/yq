<!DOCTYPE html>
<html lang="en">
  <head>

    {% block head %}
      <title>YQ</title>
      <meta name="description" value="Yet Another Golf Site" />
      <meta name="keywords" value="code-golf" />
    {% endblock head %}

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: sans-serif;
        }

        ul,
        ol {
            margin-bottom: 1rem;
        }

        .header {
            width: 100%;
            border-bottom: 1px solid black;
            display: flex;
            justify-content: space-between;
            padding: 0.5rem;
            height: 2rem;
        }

        .header img {
            width: 2rem;
            height: 2rem;
            margin-left: 1rem;
        }

        .content {
            padding: 1rem;
        }

        textarea {
            width: 100%;
            height: 8rem;
            font-family: monospace;
        }

        .test-case {
            margin-bottom: 1rem;
            padding: 1rem;
        }

        .test-case pre {
            background-color: white;
            padding: 1rem;
        }

        .test-pass {
            background-color: #7dca8c;
        }

        .test-fail {
            background-color: #ff4444;
        }

        .test-warning {
            background-color: yellow;
        }

        .test-info {
            background-color: #ddd;
        }

        .diff {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }
    </style>
  </head>
  <body>
    <div class="header">
      <div>
        <a href="/">Home</a>
      </div>
      <div>
        {% if account %}
          {{ account.username }}
          <img src="{{ account.avatar }}" alt="{{ account.username }} profile picture" />
        {% else %}
          <a href="/login/github">Login</a>
        {% endif %}
      </div>
    </div>

    <div class="content">
      {% block content %}
      {% endblock content %}
    </div>
  </body>
</html>
